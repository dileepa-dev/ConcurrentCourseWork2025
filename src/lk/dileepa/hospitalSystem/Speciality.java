package lk.dileepa.hospitalSystem;

public enum Speciality { // Represents fixed set of consultant specialities and Prevents invalid values (type safety)
    PAEDIATRICIAN,
    SURGEON,
    CARDIOLOGIST
}